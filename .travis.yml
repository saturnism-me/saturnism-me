install:
  - sudo apt-get install hugo=0.37.1
  - rm -rf public || exit 0
script:
  - hugo
  - echo 'saturnism.me' > public/CNAME
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  on:
    branch: master
